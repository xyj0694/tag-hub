# AGENTS.md — tag-hub 项目记忆

## Git 分支

- 当前工作分支：`tag-hub-mk`
- **禁止 `git push`**：没有用户明确通知，不允许推送到 GitHub（origin）
- 允许 `git commit`：本地提交随意

## 项目背景

标签接单协作平台（tag-hub），串联品牌方、平台运营、供应商三方的 B2B 业务协作系统。

- 原型代码：`docs/原型设计/prototype/`
- 原型运行：`cd docs/原型设计/prototype && npm run dev` → `http://localhost:5173`
- 产品文档：`docs/产品PRD.md`、`docs/产品需求规格.md`
- 任务看板：`docs/执行任务看板.md`
- 页面清单：`docs/原型设计/页面清单与信息架构.md`


## 原型当前状态（2026-06-13）

## 自测规则

每次修改程序后，Codex 必须先自行验证通过，再告知用户：
- dev server 必须真正可访问（跨 exec_command 二次 curl 确认 HTTP 200）
- 关键页面用 curl 拉取 HTML/JS 检查无编译报错
- 涉及数据变更时确认 mock 数据无冲突
- 确认通过后才报告用户"已完成"，不要让用户充当测试人员


### 品牌方端
- **模板管理 (B02)**：名称搜索 + SKU 搜索（AutoComplete 自动提示）+ 类型筛选，使用说明书按钮位于标题栏右侧
- **组织管理 (B09)**：采购账号管理（新增/编辑/权限配置）+ 工厂列表（只读），已去掉使用说明书
- **创建订单 — 模板下单 (B04)**：收货信息与确认页左侧新增「上一步」按钮；提交提示改为「系统已经通知供应商尽快审核和确认接单反馈」
- **订单详情 (B08)**：去掉操作记录；订单进度每个里程碑配时间；基本信息去掉「工厂」字段；SKU 明细新增「模板」列（含图稿 Popover 预览）和「数据来源」列
- 品牌方配置生效：组织管理页面 Tab 和操作按钮根据平台配置动态显隐

### 平台方端
- **配置管理 (O08)**：新增模块，含系统配置、业务配置、品牌方配置（按品牌开关 Tab 可见性和创建权限）、供应商配置 4 个子页面
- OpsBrandConfig 通过 BrandConfigContext 与品牌方端联动

### 通用
- 订单列表新增 SKU 列、dataSource 列
- 订单详情 SKU明细 card 始终可见
- 品牌方 UI 已去除所有平台内部引用（拆单、分配供应商等）